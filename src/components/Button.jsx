import React from 'react'

const Button = ({text}) => {
  return (
    <button className='rounded-[10px] bg-darkblue w-[338px] uppercase h-[83px] text-white font-bold text-[20px]'>{text}</button>
  )
}

export default Button
